FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /out/ragserver ./cmd/server

FROM alpine:3.22
WORKDIR /app
COPY --from=build /out/ragserver /app/ragserver
EXPOSE 8080
CMD ["/app/ragserver"]

